name: Frontend Build and Push

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: xiaodiyanxuan-frontend  # ✅ 正确

jobs:
  build:
    steps:
      - name: Build and push Docker image
        id: build  # ✅ 正确的 ID
        uses: docker/build-push-action@v5
        ...
      - name: Image digest
        run: echo ${{ steps.build.outputs.digest }}  # ✅ 正确的引用
