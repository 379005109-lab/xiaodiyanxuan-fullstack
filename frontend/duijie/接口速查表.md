# API 接口速查表

## 认证相关
| 方法 | 路径 | 认证 | 说明 |
|-----|------|:---:|------|
| POST | `/api/auth/wxlogin` | ❌ | 微信登录，参数: `{code}` |
| GET | `/api/user/info` | ✅ | 获取当前用户信息 |

## 商品相关
| 方法 | 路径 | 认证 | 说明 |
|-----|------|:---:|------|
| GET | `/api/home` | ❌ | 首页数据 |
| GET | `/api/goods/list` | ❌ | 商品列表，参数: `page,pageSize,category,style,sort` |
| GET | `/api/goods/:id` | ❌ | 商品详情 |
| GET | `/api/goods/search` | ❌ | 搜索，参数: `keyword` |

## 购物车相关
| 方法 | 路径 | 认证 | 说明 |
|-----|------|:---:|------|
| GET | `/api/cart` | ✅ | 获取购物车 |
| POST | `/api/cart` | ✅ | 添加到购物车，参数: `{goodsId,count,specs}` |
| PUT | `/api/cart/:cartId` | ✅ | 更新数量，参数: `{count}` |
| DELETE | `/api/cart/:cartId` | ✅ | 删除商品 |

## 订单相关
| 方法 | 路径 | 认证 | 说明 |
|-----|------|:---:|------|
| POST | `/api/orders` | ✅ | 创建订单，参数: `{type,goods,totalPrice,receiver,remark}` |
| GET | `/api/orders` | ✅ | 订单列表，参数: `status,page,pageSize` |
| GET | `/api/orders/:orderId` | ✅ | 订单详情 |
| POST | `/api/orders/:orderId/cancel` | ✅ | 取消订单，参数: `{reason}` |
| POST | `/api/orders/:orderId/confirm` | ✅ | 确认收货 |

## 收藏相关
| 方法 | 路径 | 认证 | 说明 |
|-----|------|:---:|------|
| GET | `/api/favorites` | ✅ | 收藏列表 |
| POST | `/api/favorites` | ✅ | 添加收藏，参数: `{goodsId}` |
| DELETE | `/api/favorites/:goodsId` | ✅ | 取消收藏 |

## 地址相关
| 方法 | 路径 | 认证 | 说明 |
|-----|------|:---:|------|
| GET | `/api/addresses` | ✅ | 地址列表 |
| POST | `/api/addresses` | ✅ | 添加地址 |
| PUT | `/api/addresses/:addressId` | ✅ | 更新地址 |
| DELETE | `/api/addresses/:addressId` | ✅ | 删除地址 |

## 砍价相关
| 方法 | 路径 | 认证 | 说明 |
|-----|------|:---:|------|
| GET | `/api/bargain/goods` | ❌ | 砍价商品列表 |
| POST | `/api/bargain/start` | ✅ | 发起砍价，参数: `{goodsId}` |
| GET | `/api/bargain/my` | ✅ | 我的砍价 |
| POST | `/api/bargain/:bargainId/help` | ✅ | 帮忙砍价 |

## 优惠券相关
| 方法 | 路径 | 认证 | 说明 |
|-----|------|:---:|------|
| GET | `/api/coupons` | ✅ | 优惠券列表，参数: `status` |

---

## 订单状态码
| 状态码 | 说明 |
|:-----:|------|
| 1 | 待付款 |
| 2 | 待发货 |
| 3 | 待收货 |
| 4 | 已完成 |
| 5 | 已取消 |

## 响应格式
```json
{ "code": 0, "data": {}, "message": "success" }
```

## 认证头
```
Authorization: Bearer <token>
```
