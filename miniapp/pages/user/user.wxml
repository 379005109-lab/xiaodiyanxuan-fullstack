<!--我的页面-->
<view class="container">
  <!-- 用户信息 -->
  <view class="user-header">
    <view class="user-info" wx:if="{{isLogin}}">
      <image class="avatar" src="{{userInfo.avatar || '/images/default-avatar.png'}}" mode="aspectFill"/>
      <view class="info">
        <text class="nickname">{{userInfo.nickname || '小迪用户'}}</text>
        <text class="phone" wx:if="{{userInfo.phone}}">{{userInfo.phone}}</text>
      </view>
    </view>
    <view class="login-btn" wx:else bindtap="handleLogin">
      <image class="avatar" src="/images/default-avatar.png" mode="aspectFill"/>
      <text>点击登录</text>
    </view>
  </view>

  <!-- 订单入口 -->
  <view class="order-section">
    <view class="section-header" bindtap="goOrders">
      <text class="section-title">我的订单</text>
      <text class="section-more">全部订单 ></text>
    </view>
    <view class="order-nav">
      <view class="order-item" bindtap="goOrders" data-status="1">
        <text class="order-icon">💳</text>
        <text>待付款</text>
        <text class="badge" wx:if="{{orderCount.unpaid > 0}}">{{orderCount.unpaid}}</text>
      </view>
      <view class="order-item" bindtap="goOrders" data-status="2">
        <text class="order-icon">📦</text>
        <text>待发货</text>
        <text class="badge" wx:if="{{orderCount.unshipped > 0}}">{{orderCount.unshipped}}</text>
      </view>
      <view class="order-item" bindtap="goOrders" data-status="3">
        <text class="order-icon">🚚</text>
        <text>待收货</text>
        <text class="badge" wx:if="{{orderCount.shipped > 0}}">{{orderCount.shipped}}</text>
      </view>
      <view class="order-item" bindtap="goOrders" data-status="4">
        <text class="order-icon">✅</text>
        <text>已完成</text>
      </view>
    </view>
  </view>

  <!-- 功能菜单 -->
  <view class="menu-section">
    <view class="menu-item" bindtap="goAddress">
      <text class="menu-icon">📍</text>
      <text class="menu-text">收货地址</text>
      <text class="menu-arrow">></text>
    </view>
    <view class="menu-item" bindtap="goFavorite">
      <text class="menu-icon">❤️</text>
      <text class="menu-text">我的收藏</text>
      <text class="menu-arrow">></text>
    </view>
    <view class="menu-item" bindtap="contactService">
      <text class="menu-icon">💬</text>
      <text class="menu-text">联系客服</text>
      <text class="menu-arrow">></text>
    </view>
  </view>

  <!-- 退出登录 -->
  <view class="logout-btn" wx:if="{{isLogin}}" bindtap="logout">
    退出登录
  </view>
</view>
