<navigation-bar title="" back="{{false}}" color="white" background="transparent"></navigation-bar>
<scroll-view class="page-scroll" scroll-y>
<view class="page">
	<!-- 全屏Hero - 绿色背景沙发图 -->
	<view class="hero-fullscreen">
		<view class="hero-bg-gradient"></view>
		<view class="hero-d-letter">D</view>
		<image class="hero-sofa-image" src="{{homeBannerImage || 'https://images.unsplash.com/photo-1493663284031-b7e3aefcae8e?w=800'}}" mode="aspectFit"></image>
		<!-- 标语 -->
		<view class="hero-slogan">
			<view class="slogan-line"><text class="bold">{{i18n.home.slogan1 || '线上选款'}}</text></view>
			<view class="slogan-line"><text class="bold">{{i18n.home.slogan2 || '展厅体验'}}</text></view>
		</view>
		<view class="hero-price-tag"><text class="bold">{{i18n.home.priceTag || '一口价'}}</text></view>
		<view class="hero-arrow">↓</view>
	</view>

	<!-- 陪买服务卡片 -->
	<view class="service-card" bindtap="goGuideService">
		<view class="service-title">{{i18n.home.serviceTitle || '陪买服务'}}</view>
		<view class="service-desc">{{i18n.home.serviceDesc || '专业顾问一对一陪同选购，为您甄选最适合的家居方案。'}}</view>
		<view class="service-btn">
			<text>{{i18n.home.bookService || '预约服务'}}</text>
			<text class="arrow">›</text>
		</view>
	</view>

	<!-- 快捷入口 -->
	<view class="quick-entries">
		<view class="quick-item" bindtap="goBargain">
			<view class="quick-icon bargain">🏷️</view>
			<view class="quick-value">50%</view>
			<view class="quick-label">{{i18n.home.discount || '限时折扣'}}</view>
		</view>
		<view class="quick-item" bindtap="goPackage">
			<view class="quick-icon package">📦</view>
			<view class="quick-value">{{i18n.home.packageEntry || '套餐'}}</view>
			<view class="quick-label">{{i18n.home.oneStop || '一站式购齐'}}</view>
		</view>
	</view>

</view>
</scroll-view>
