<!-- 我的套餐页 — Apple 风格 -->
<navigation-bar title="我的套餐" back="{{true}}" color="black" background="#FFFFFF"></navigation-bar>
<view class="page">
  <view class="content">
    <view class="pkg-list" wx:if="{{packages.length > 0}}">
      <block wx:for="{{packages}}" wx:key="_id">
        <view class="pkg-card" bindtap="goDetail" data-id="{{item._id || item.id}}">
          <view class="pkg-top">
            <image class="pkg-img" src="{{item.image || item.coverImage}}" mode="aspectFill"></image>
            <view class="pkg-info">
              <view class="pkg-name">{{item.name}}</view>
              <view class="pkg-tags">
                <text class="pkg-tag" wx:if="{{item.style}}">{{item.style}}</text>
                <text class="pkg-tag" wx:if="{{item.roomType}}">{{item.roomType}}</text>
              </view>
              <view class="pkg-price">¥{{item.totalPrice || item.price}}</view>
            </view>
          </view>
          <view class="pkg-status">
            <text class="status-text {{item.status}}">{{item.statusText || '进行中'}}</text>
            <text class="pkg-date">{{item.createTime || item.createdAt}}</text>
          </view>
        </view>
      </block>
    </view>

    <view class="empty-state" wx:if="{{packages.length === 0 && !loading}}">
      <text class="empty-icon">📦</text>
      <text class="empty-title">暂无套餐订单</text>
      <text class="empty-hint">去选一套心仪的家具套餐吧</text>
      <view class="empty-btn" bindtap="goBrowse">去逛逛</view>
    </view>
  </view>
</view>
