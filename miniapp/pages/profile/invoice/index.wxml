<!-- 发票管理页 — Apple 风格 -->
<navigation-bar title="发票管理" back="{{true}}" color="black" background="#FFFFFF"></navigation-bar>
<view class="page">
  <view class="content">
    <view class="invoice-list" wx:if="{{invoices.length > 0}}">
      <block wx:for="{{invoices}}" wx:key="_id">
        <view class="invoice-card" bindtap="goEdit" data-id="{{item._id || item.id}}">
          <view class="invoice-type-tag">{{item.type === 'company' ? '企业' : '个人'}}</view>
          <view class="invoice-info">
            <view class="invoice-title-text">{{item.title || item.name}}</view>
            <view class="invoice-tax" wx:if="{{item.taxNumber}}">税号：{{item.taxNumber}}</view>
          </view>
          <view class="invoice-actions">
            <view class="invoice-default" wx:if="{{item.isDefault}}">默认</view>
            <text class="invoice-arrow">›</text>
          </view>
        </view>
      </block>
    </view>

    <view class="empty-state" wx:if="{{invoices.length === 0 && !loading}}">
      <text class="empty-icon">🧾</text>
      <text class="empty-text">暂无发票抬头</text>
    </view>

    <view class="add-btn" bindtap="goAdd">
      <text>+ 新增发票抬头</text>
    </view>
  </view>
</view>
