# 小迪严选 UI 设计规范

## 🎨 主题色

### 主色（深绿）
| 名称 | 色值 | 用途 |
|------|------|------|
| 主色 | `#14452F` | 按钮、导航、重要操作 |
| 主色浅 | `#166534` | 悬停状态 |

### 强调色（金色）
| 名称 | 色值 | 用途 |
|------|------|------|
| 强调色 | `#D6AD60` | 价格、高亮、促销标签 |
| 强调色浅 | `#E8C98D` | 金色悬停 |
| 强调色深 | `#B99240` | 金色按下 |

### 中性色（Stone系列）
| 名称 | 色值 | 用途 |
|------|------|------|
| 背景色 | `#F2F4F3` | 页面背景 |
| 卡片背景 | `#FFFFFF` | 卡片、弹窗 |
| 分割线 | `#E6E8E9` | 边框、分割 |
| 次要文字 | `#B0C4DF` | 提示文字、描述 |
| 正文文字 | `#2A3C36` | 标题、正文 |
| 深色文字 | `#14452F` | 重要标题 |

---

## 📝 字体规范

### 字体族
```css
font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Helvetica Neue", Arial, sans-serif;
```

### 字号规范
| 用途 | 大小 | 字重 |
|------|------|------|
| 大标题 | 36rpx | 700 (Bold) |
| 标题 | 32rpx | 600 (Semibold) |
| 正文 | 28rpx | 400 (Regular) |
| 描述 | 26rpx | 400 |
| 小字 | 24rpx | 400 |
| 价格 | 32-40rpx | 700 |

---

## 📐 圆角规范

| 元素 | 圆角 |
|------|------|
| 按钮 | 8rpx |
| 卡片 | 16rpx |
| 图片 | 12rpx |
| 标签 | 6rpx |
| 全圆按钮 | 9999rpx |

---

## 📏 间距规范

| 类型 | 尺寸 |
|------|------|
| 页面边距 | 32rpx |
| 卡片间距 | 24rpx |
| 元素间距 | 16rpx |
| 紧凑间距 | 8rpx |

---

## 🔘 按钮样式

### 主按钮
```css
.btn-primary {
  background: #14452F;
  color: #FFFFFF;
  border: none;
  border-radius: 8rpx;
  font-weight: 600;
}
```

### 强调按钮
```css
.btn-accent {
  background: #D6AD60;
  color: #FFFFFF;
  border: none;
  border-radius: 8rpx;
  font-weight: 600;
}
```

### 边框按钮
```css
.btn-outline {
  background: #FFFFFF;
  color: #14452F;
  border: 2rpx solid #14452F;
  border-radius: 8rpx;
}
```

---

## 🏷️ 标签样式

### NEW标签
```css
.new-badge {
  position: absolute;
  top: 16rpx;
  left: 16rpx;
  padding: 8rpx 16rpx;
  background: #14452F;
  color: #FFFFFF;
  font-size: 22rpx;
  font-weight: 600;
  border-radius: 6rpx;
}
```

### 价格标签
```css
.price-tag {
  background: rgba(214, 173, 96, 0.15);
  color: #B99240;
  padding: 4rpx 12rpx;
  border-radius: 6rpx;
  font-size: 22rpx;
}
```

---

## 🃏 卡片样式

```css
.card {
  background: #FFFFFF;
  border-radius: 16rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.06);
}
```

---

## 📱 CSS变量

在 `app.wxss` 中定义的全局CSS变量：

```css
page {
  /* 主色(深绿) */
  --color-primary: #14452F;
  --color-primary-light: #166534;
  
  /* 强调色(金色) */
  --color-accent: #D6AD60;
  --color-accent-light: #E8C98D;
  --color-accent-dark: #B99240;
  
  /* 中性色(Stone系列) */
  --color-bg: #F2F4F3;
  --color-card: #FFFFFF;
  --color-border: #E6E8E9;
  --color-text-secondary: #B0C4DF;
  --color-text: #2A3C36;
  --color-text-dark: #14452F;
}
```

---

## 📄 页面结构

### 页面背景
所有页面使用 `var(--color-bg)` 作为背景色。

### 内容区域
- 页面边距：32rpx
- 卡片间距：24rpx
- 底部安全区：`env(safe-area-inset-bottom)`

### 导航栏
- 高度：44px + 状态栏高度
- 背景：白色
- 标题：居中，32rpx，600字重

---

## 🎯 设计原则

1. **一致性**：所有页面使用统一的颜色、字体、间距
2. **简洁性**：减少视觉噪音，突出核心内容
3. **可读性**：确保文字与背景有足够对比度
4. **响应式**：使用rpx单位适配不同屏幕
5. **品牌感**：深绿+金色的配色体现品质感

---

## 📋 更新日志

- **2024-12-03**：初始版本，完成全局样式规范
